<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:TodoFrontend.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="TodoFrontend.TodoListView"
             x:DataType="vm:MainWindowViewModel">
  <Grid Margin="20"
        RowDefinitions="Auto,Auto,Auto,*"
        RowSpacing="12">

        

        <Button Grid.Row="0"
                Content="管理画面へ"
                Command="{Binding ShowManagementCommand}" />

        <Border Grid.Row="1"
            Padding="12"
            BorderThickness="1"
            BorderBrush="Gray">
            <StackPanel Spacing="8">
                <TextBlock Text="絞り込み" FontSize="18" FontWeight="Bold" />

                <TextBox Watermark="Project名で絞り込み"
                    Text="{Binding FilterProjectName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBox Watermark="Team名で絞り込み"
                    Text="{Binding FilterTeamName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button Content="絞り込み"
                        Command="{Binding ApplyFilterCommand}" />

                    <Button Content="クリア"
                        Command="{Binding ClearFilterCommand}" />
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="2"
                Padding="12"
                Margin="0,0,0,12"
                BorderThickness="1"
                BorderBrush="Gray">

            <StackPanel Spacing="8">
                <TextBlock Text="Todo追加"
                           FontSize="18"
                           FontWeight="Bold" />

                <TextBox Watermark="タイトル"
                         Text="{Binding NewTodoTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBox Watermark="TeamName"
                         Text="{Binding NewTodoTeamName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBox Watermark="MemberName"
                         Text="{Binding NewTodoMemberNames, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <Button Content="追加"
                        Command="{Binding AddTodoCommand}" />
            </StackPanel>
        </Border>

        <!--
          一覧エリアは状態ごとに表示を切り替える。
          エラーが出ても既存Todoがある場合はListBoxを残すため、エラー表示と一覧表示は別々の条件にしている。
        -->
        <Grid Grid.Row="3">
        <TextBlock IsVisible="{Binding IsLoading}"
                   Text="読み込み中です..."
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="Gray" />

        <TextBlock IsVisible="{Binding ShowErrorState}"
                   Text="{Binding ErrorMessage}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   TextWrapping="Wrap"
                   Foreground="#B42318" />

        <TextBlock IsVisible="{Binding ShowEmptyState}"
                   Text="表示できるTodoはありません。"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="Gray" />

        <ListBox IsVisible="{Binding IsTodoListVisible}"
                 ItemsSource="{Binding Todos}">

            <ListBox.ItemTemplate>
    <!--
      DataTemplate内のBinding先はMainWindowViewModelではなくTodoItemViewModel。
      x:DataTypeを付けると、TitleやStatusなどのBindingミスに気づきやすくなる。
    -->
    <DataTemplate x:DataType="vm:TodoItemViewModel">
        <Border Padding="12"
                Margin="0,6"
                BorderThickness="1"
                BorderBrush="Gray"
                CornerRadius="6">

            <StackPanel Spacing="6">

                <!-- Todo名 -->
                <TextBlock Text="{Binding Title}"
                           FontSize="18"
                           FontWeight="Bold" />

                <!-- 担当チーム -->
                <StackPanel Orientation="Horizontal"
                            Spacing="6">
                    <TextBlock Text="担当チーム:"
                               FontWeight="Bold" />
                    <TextBlock Text="{Binding TeamName}" />
                </StackPanel>

                <!-- 担当メンバー -->
                <StackPanel Orientation="Horizontal"
                            Spacing="6">
                    <TextBlock Text="担当メンバー:"
                               FontWeight="Bold" />
                    <TextBlock Text="{Binding MemberNames}" />
                </StackPanel>

                <!-- ステータス -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center"
                            Spacing="6">
                    <TextBlock Text="ステータス:" VerticalAlignment="Center"
                               FontWeight="Bold" />
                    <TextBlock Text="{Binding Status}" VerticalAlignment="Center" />
                </StackPanel>

                <!-- 対象プロジェクト -->
                <StackPanel Orientation="Horizontal"
                            Spacing="6">
                    <TextBlock Text="対象プロジェクト:"
                               FontWeight="Bold" />
                    <TextBlock Text="{Binding ProjectName}" />
                </StackPanel>

                <!-- 操作ボタン -->
                <StackPanel Orientation="Horizontal"
                            Spacing="8"
                            Margin="0,8,0,0">
                    <Button Content="完了"
                            Command="{Binding DataContext.UpdateTodoCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                            CommandParameter="{Binding Id}" />

                    <Button Content="削除"
                            Command="{Binding DataContext.DeleteTodoCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                            CommandParameter="{Binding Id}" />
                </StackPanel>

            </StackPanel>
        </Border>
    </DataTemplate>
</ListBox.ItemTemplate>
        </ListBox>
        </Grid>

    </Grid>
</UserControl>
